export enum PackageType{
    TimeAndMessage = 1,
    MessageOnly = 2,
    UnlimitedMessageAmount = -1 
}

export enum PackageTypeLabel{
    TimeAndMessage = 'Duy trì dịch vụ',
    MessageOnly = 'Số lượng tin nhắn',
}